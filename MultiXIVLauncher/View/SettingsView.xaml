<UserControl x:Class="MultiXIVLauncher.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="Transparent">

    <Grid Background="Transparent" Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Paramètres du Launcher"
                   FontSize="20"
                   FontWeight="Bold"
                   Foreground="{StaticResource ClrAccent2}"
                   Margin="0,0,0,12"/>

        <Border Grid.Row="1"
                Background="{StaticResource ClrBgCard}"
                CornerRadius="10"
                Padding="20"
                Effect="{StaticResource GlowEffect}">

            <StackPanel Orientation="Vertical">
                <StackPanel Orientation="Vertical" Margin="0,0,0,16">
                    <TextBlock Text="Indiquez l'emplacement de votre fichier XIVLauncher.exe :"
                               Style="{StaticResource BodyText}"
                               Margin="0,0,0,6"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <Border Grid.Column="0"
                                CornerRadius="6"
                                BorderBrush="{StaticResource ClrAccent}"
                                BorderThickness="1"
                                Background="#191B2A"
                                Margin="0,0,8,0">
                            <TextBox x:Name="XIVLauncherPath"
                                     Height="32"
                                     VerticalContentAlignment="Center"
                                     Padding="6,0"
                                     Background="Transparent"
                                     BorderThickness="0"
                                     Foreground="{StaticResource ClrTextPrimary}"/>
                        </Border>

                        <Button Grid.Column="1"
                                Content="..."
                                Width="36"
                                Height="32"
                                Style="{StaticResource RoundedButton}"
                                ToolTip="Parcourir"
                                Click="BrowseXIVLauncher"/>
                    </Grid>
                </StackPanel>

                <StackPanel Orientation="Vertical" Margin="0,0,0,16">
                    <TextBlock Text="Langue du launcher :"
                               Style="{StaticResource BodyText}"
                               Margin="0,0,0,6"/>
                    <ComboBox x:Name="LanguageSelect"
                              Width="200"
                              Height="32"
                              HorizontalAlignment="Left"
                              VerticalAlignment="Center">
                        <ComboBoxItem Content="Français" Tag="fr"/>
                        <ComboBoxItem Content="English" Tag="en"/>
                        <ComboBoxItem Content="Deutsch" Tag="de"/>
                    </ComboBox>
                </StackPanel>

                <StackPanel Orientation="Vertical" Margin="0,0,0,8">
                    <TextBlock Text="Clé Dalamud Beta :"
                               Style="{StaticResource BodyText}"
                               Margin="0,0,0,6"/>
                    <Border CornerRadius="6"
                            BorderBrush="{StaticResource ClrAccent}"
                            BorderThickness="1"
                            Background="#191B2A"
                            Margin="0,0,0,8">
                        <TextBox x:Name="DalamudBetaKey"
                                 Height="32"
                                 VerticalContentAlignment="Center"
                                 Padding="6,0"
                                 Background="Transparent"
                                 BorderThickness="0"
                                 Foreground="{StaticResource ClrTextPrimary}"
                                 IsReadOnly="False"
                                 Text=""/>
                    </Border>
                </StackPanel>

                <StackPanel Orientation="Vertical">
                    <TextBlock Text="Type de version Dalamud Beta :"
                               Style="{StaticResource BodyText}"
                               Margin="0,0,0,6"/>
                    <Border CornerRadius="6"
                            BorderBrush="{StaticResource ClrAccent}"
                            BorderThickness="1"
                            Background="#191B2A">
                        <TextBox x:Name="DalamudBetaKind"
                                 Height="32"
                                 VerticalContentAlignment="Center"
                                 Padding="6,0"
                                 Background="Transparent"
                                 BorderThickness="0"
                                 Foreground="{StaticResource ClrTextPrimary}"
                                 IsReadOnly="False"
                                 Text=""/>
                    </Border>
                </StackPanel>

            </StackPanel>
        </Border>
    </Grid>
</UserControl>
